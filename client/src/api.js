// src/api.js
// One shared constant for where the backend lives. Every page that needs
// to call the API imports this instead of typing the URL out each time.

export const API_BASE = 'http://localhost:3000';
