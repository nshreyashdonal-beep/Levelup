// src/pages/Home.jsx
// A placeholder page — just enough to prove the router is working.
// Real content (course listings, etc.) gets built as its own checklist piece.

function Home() {
  return (
    <div className="page">
      <h1>LevelUp</h1>
      <p>The frontend skeleton is working — routing is set up and ready to grow.</p>
    </div>
  )
}

export default Home
